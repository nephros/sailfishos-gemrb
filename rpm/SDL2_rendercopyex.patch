diff --git a/gemrb/plugins/SDLVideo/SDL20Video.h b/gemrb/plugins/SDLVideo/SDL20Video.h
index 0b7c3ef43..cb877691d 100644
--- a/gemrb/plugins/SDLVideo/SDL20Video.h
+++ b/gemrb/plugins/SDLVideo/SDL20Video.h
@@ -141,7 +141,7 @@ public:
 		SDL_Renderer* targetRenderer = static_cast<SDL_Renderer*>(display);
 		SDL_Rect dst = RectFromRegion(rect);
 		SDL_SetTextureBlendMode(texture, SDL_BLENDMODE_BLEND);
-		int ret = SDL_RenderCopy(targetRenderer, texture, nullptr, &dst);
+		int ret = SDL_RenderCopyEx(targetRenderer, texture, nullptr, &dst, 90.0, NULL, SDL_FLIP_NONE);
 		if (ret != 0) {
 			Log(ERROR, "SDLVideo", "{}", SDL_GetError());
 		}
